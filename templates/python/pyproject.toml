[project]
name = "NAME"
version = "0.0.1"
requires-python = ">=3.9"
description = "A new Python project scaffolded by proj new python."

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
